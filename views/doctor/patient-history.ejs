<% layout("/layouts/doctor/boilerplate.ejs") %>

<div class="container col-10 offset-1 mt-4">
<h3 class="text-center mt-4 mb-4">My Patients - Consult & Prescribe</h3>
    <% if (typeof allPatients !== 'undefined' && allPatients.length > 0) { %>
<p class="text-center text-muted mb-4">Click "Consult & Prescribe" to consult patients (<%- allPatients.length %> patients)</p>
    <% } else { %>
    <div class="text-center py-5">
      <i class="fa-solid fa-users fa-3x text-muted mb-3"></i>
      <p class="text-muted">No patients assigned yet.</p>
    </div>
    <% } %>
    <hr class="mb-4" />
    <div class="row">
    <% for(let patient of allPatient) {%>
                <div class="col-md-6 col-lg-4 mb-4">
                  <div class="card h-100 shadow-sm border-0">
                    <div class="card-body text-center p-4">
                      <i class="fa-solid fa-user-injured fa-2x text-primary mb-3"></i>
                <div class="card-body">
                    <h5 class="card-title"><%= patient.name %></h5>
                    <p class="">Age :<%= patient.age %> </p>

class="btn btn-primary btn-sm">Consult & Prescribe</a>
                </div>
            </div>
    <% } %>
</div>
</div>